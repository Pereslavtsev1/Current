spring:
  datasource:
    url: jdbc:h2:mem:test
    username: SA
    password: password
  jpa:
    defer-datasource-initialization: true
  sql:
    init:
      mode: always

  mail:
    host: 3025
    username: springboot
    password: secret
  h2:
    console:
      enabled: true